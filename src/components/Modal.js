import { View, Text, Modal } from "react-native";
import React from "react";

export default function Modal(message, visible, setVisible) {
  return (
    <View>
      <Text>Modal</Text>
    </View>
  );
}
